version: "3.8"

services:
  db:
    image: postgres:15
    container_name: evalynx_db
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: pravinwin4
      POSTGRES_DB: evalynx
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
